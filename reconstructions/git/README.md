# Git reconstructions

Content below this directory is reconstructed from public BambuStudio Git trees. It is never
classified as or merged with verified OTA content. A matching `BBL.json` version is not proof
that Bambu published an OTA archive, and an OTA with that version may contain different bytes.

